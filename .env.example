# Dominio público para canonical URLs y vistas previas.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# `npx supabase status` muestra los valores de la pila local.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54371
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=pegar-publishable-o-anon-key-local

# Solo servidor. Nunca expongas esta clave en variables NEXT_PUBLIC_*.
SUPABASE_SERVICE_ROLE_KEY=pegar-service-role-key-local
